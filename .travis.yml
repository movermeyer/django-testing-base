language: python
python:
  - 3.4
  - 3.3
env:
  - DJANGO_VERSION=1.6
  - DJANGO_VERSION=1.7
install:
  - pip install -q django==$DJANGO_VERSION selenium
  - pip install .
script:
  - testsite/manage.py test testapp

